apiVersion: v1
kind: Service
metadata:
  name: delbot-frontend-service
  labels:
    app: delbot-frontend
    component: frontend
spec:
  selector:
    app: delbot-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
